Reader = {
    Input = function (OldPad, Pad, OldTouch, Touch)
        
    end,
    Update = function (delta)
        
    end,
    Draw = function ()
        Screen.clear(Color.new(255, 255, 255))
    end
}